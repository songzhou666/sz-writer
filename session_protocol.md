# 会话状态卡内联协议（内联状态卡）

> 背景：传统"接力棒落盘 + 中间产物文件"模型依赖可写文件系统，而纯对话智能体默认无持久文件系统。
> 本协议把状态持久化**整体降级为"会话内冗余状态卡"**——状态全部内联在回复文本中，仅靠最近几轮消息即可重建，零文件依赖。

## 1. 为什么内联
- 纯对话环境下会话上下文可控、可续聊；但服务端不保证能写/读任意文件。
- 内联状态卡让"断点续写、修订模式、阶段切换"在纯对话下成立。

## 2. 状态卡格式（回复首行固定）
```
【当前阶段】S4 文种选型　【下一步】请确认选型建议(1/2/3)
【写作画像】大类=未定 小类=待判 | 方向=上行 | 行业=基层乡镇 | 场景=向县财政局申请专项经费
　　　　　　等级=正式 | 用途=上报 | 岗位=办公室 | 补充=发文XX街道办 主送县财政局
```
- 首行固定字段：`【当前阶段】` 必填；`【下一步】` 说明用户该做什么；S2 之后连同 `【写作画像】`；进入付费流程后追加 `【支付状态】`。
- 阶段取值：S0 意图 → S1 安全 → S2 画像 → S3 追问 → S4 选型 → S5 确认 → S6 起草/改写 → S7 审核；评估=C 类独立出口《应用文评估报告》（不经阻断流水线）；联网扩展标记为 `S4→联网扩展`。

### 支付状态字段（付费 Skill 专用）

进入资产加载流程后，状态卡追加 `【支付状态】` 字段，用于跨轮次追踪付费状态：

```
【支付状态】已付费 | trade_no=2021xxxx | 凭证已保存，后续调用复用
```

| 状态值 | 含义 | Agent 行为 |
| --- | --- | --- |
| 未付费 | 尚未发起支付或支付未完成 | 按 §9.1 流程调起支付 |
| 已付费 | 支付成功，Payment-Proof 已保存 | 所有后续资产调用复用同一 Payment-Proof，**禁止重新拉起支付** |
| 凭证失效 | 服务端返回 400（`INVALID_PAYMENT_PROOF`） | 提示用户重新支付，重新走 402→支付流程 |
| 支付失败 | 用户取消或支付超时 | 终止当前资产加载，提示用户重新发起 |

> **Payment-Proof 本身不写入状态卡**（避免在对话中暴露支付凭证），由 Agent 在内存/上下文中保存。状态卡仅记录支付状态和 trade_no。

## 3. 内联承载的中场产物（也以文本呈现，不入文件）
| 产物 | 呈现方式 |
| --- | --- |
| 写作画像单 | 状态卡内联 + 需用户修改时可整段展示让用户改 |
| 追问问题卡 | 回复正文（≤4 问） |
| 选型建议卡 | 回复正文（固定格式，见 SKILL.md §S4） |
| 六维审核报告 | 回复正文（含阻断/警告/证据引用） |
| 应用文评估报告 | 回复正文（模板见 rules/eval_report_template.md） |
| 临时文体资产卡 | 回复正文（模板见 rules/temp_asset_template.md） |
| 交付终稿 | 回复正文（可直接复制） |

## 4. 断点续写（不依赖文件）
- **同会话**：靠状态卡在最近消息中重建；每轮首行就是最新状态，无需额外恢复动作。
- **新会话**：检测到无状态卡（首轮无 `【当前阶段】` 上下文）→ 按新任务从 S0 开始；若用户说"继续上次"但新会话无上下文 → 明确告知"跨会话无法自动带状态，请粘贴上次的选型建议卡/画像单或重新描述"。
- **长时间中断后续写（>120 分钟）**：上次的 Payment-Proof 已过期，服务端返回 400（`INVALID_PAYMENT_PROOF`）。提示用户"距上次支付已超过 120 分钟，本次写作凭证已过期，需重新支付"，然后**不带凭证**重新调用 API 发起新支付。

## 5. 修订模式
交付终稿后用户请求"改第三段 / 再润色"：
- 视为当前任务修订，**保留画像单与文种，不重走 S4/S5**，从 S6 起草/改写直接进入，状态卡停留在 S6。
- 用户要求"写另一篇"才重置回 S0（清空画像）。

## 6. 一致性约束
- 状态卡必须与回复内容同步更新，禁止"口说 S5 已确认、实际又在 S4 重新选型"。
- 用户选了 S5 选项 2（保留原文体）后，后续不得悄悄改回推荐文种（可再提示一次，不反复纠缠）。
